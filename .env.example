# ─── Required ─────────────────────────────────────────────────────────────────
# Your Discord bot token (from https://discord.com/developers/applications)
DISCORD_BOT_TOKEN=

# Your Discord application/client ID
DISCORD_CLIENT_ID=

# Any long random string used to sign sessions
SESSION_SECRET=change_me_to_a_long_random_string

# ─── Optional ─────────────────────────────────────────────────────────────────
# HTTP port for the health-check endpoint (defaults to 8080 if not set)
# PORT=8080

# Secret key for the /deposit HTTP endpoint (used by the Roblox Luau script).
# Set this to a long random string. The Roblox script must send the same value
# as:  Authorization: Bearer <DEPOSIT_SECRET>
# Leave empty to disable the endpoint (all requests will be rejected).
DEPOSIT_SECRET=change_me_to_a_long_random_string

# Absolute path to the SQLite database file.
# Default: bot.db in the working directory (artifacts/api-server/bot.db).
# Set this on WispByte / VPS to a path on a persistent volume so data
# survives container restarts and re-deploys.
# DATABASE_PATH=/data/bot.db
