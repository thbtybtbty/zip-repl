I want to implement a hidden feature in /blackjack command called "The Invisible Look-Ahead Rigging Algorithm" to artificially increase the house edge by an approximate net 7.5% across a simulation of millions of hands. To avoid detection via data analysis (like fixed-card bias or predictable bust spikes on the first hit), the rigging must be completely invisible, smooth, and distributed dynamically.

Please implement the game loop following these exact architectural specifications:

1. Hand Initialization & Look-Ahead Phase:
- At the start of every hand, before the player sees anything, the engine must run a hidden "ghost simulation" of the hand based on the current order of the deck, assuming the player follows perfect basic strategy.
- Scenario A: If the ghost simulation results in a DEALER WIN or a PUSH, do nothing. Let the hand play out 100% naturally using pure RNG.
- Scenario B: If the ghost simulation results in a PLAYER WIN, roll a 1000-sided virtual die (1 to 1000). If the result is between 1 and 112 (11.2% static probability to yield roughly an approximate net 7.5% financial edge across the simulation), set the hidden state flag: `isHandSentenced = true`. Otherwise, let it play naturally.

2. Dynamic Intervention (Only if isHandSentenced === true):
The engine must smoothly intercept the deck to force a dealer win, ensuring that the trigger is never wasted even if the player doesn't hold a 14-16 hand:
- If the player hits on a hard hand (12-16), anytime in the game, intercept that hit. Swap the next card for a value card that will force a "Bust" (random card between the ones that force a bust), then set `isHandSentenced = false`.
- If the player stands safely (regardless of their hand score, whether it's 12, 18, or 20), wait for the dealer's turn. When the dealer draws cards, DO NOT wait to see if the dealer busts. Instead, check the dealer's final required score to beat the player. Intercept the dealer's drawing phase and swap the cards so the dealer organically reaches a final score that beats the player's final score by at least 1 point (points randomized between the winning scores, e.g., if player stood on 18, force the dealer's cards to hit a 19, 20, or 21). Once the dealer takes the winning card, set `isHandSentenced = false`.

Also, update the /simulate command, so if you select blackjack, the player will play with basic strategy (hit, stand, split or double down) depending on what he got. And take in consideration from the simulate command all the rules that are in the blackjack command. The simulate command will not include side bets